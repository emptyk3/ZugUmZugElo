import { login } from "../auth-actions"; import { ActionForm } from "../form-submit";
export default async function Page({searchParams}:{searchParams:Promise<{next?:string}>}){const {next="/"}=await searchParams;return <main className="account-shell"><h1>Anmelden</h1><p>Mit E-Mail-Adresse oder aktuellem Alias anmelden.</p><ActionForm action={login} submitLabel="Anmelden"><input type="hidden" name="next" value={next}/><label>E-Mail oder Alias<input name="identifier" required autoComplete="username" /></label><label>Passwort<input name="password" type="password" required autoComplete="current-password" /></label></ActionForm><p><a href="/passwort-vergessen">Passwort vergessen?</a></p><p><strong>Noch kein Konto?</strong> <a href="/registrieren">Jetzt registrieren</a></p></main>}
