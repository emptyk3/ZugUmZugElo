import { resetPassword } from "../auth-actions"; import { ActionForm } from "../form-submit";
export default async function Page({searchParams}:{searchParams:Promise<{token?:string}>}){const {token=""}=await searchParams;return <main className="account-shell"><h1>Passwort zurücksetzen</h1><ActionForm action={resetPassword} submitLabel="Passwort ändern"><input type="hidden" name="token" value={token}/><label>Neues Passwort<input name="password" type="password" minLength={4} required /></label><label>Passwort wiederholen<input name="passwordRepeat" type="password" minLength={4} required /></label></ActionForm></main>}
