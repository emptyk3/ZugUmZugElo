import { prisma } from "@/lib/prisma";import { requireAdmin } from "@/lib/auth/session";import { setTemporaryPassword } from "../actions";
export const dynamic="force-dynamic";export default async function Page(){await requireAdmin();const users=await prisma.user.findMany({where:{deletedAt:null,status:{not:"DELETED"}},orderBy:{email:"asc"},select:{id:true,email:true,player:{select:{alias:true}}}});return <main className="account-shell"><h1>Temporäres Passwort setzen</h1><p>Das Passwort wird ausschließlich als bcrypt-Hash gespeichert und weder protokolliert noch im AuditLog abgelegt.</p><form action={setTemporaryPassword} className="account-form"><label>Benutzer<select name="userId" required><option value="">Auswählen</option>{users.map(u=><option key={u.id} value={u.id}>{u.player?.alias??u.email}</option>)}</select></label><label>Temporäres Passwort<input name="temporaryPassword" type="password" minLength={4} required/></label><label><input name="confirmed" type="checkbox" required/> Ich bestätige das Zurücksetzen.</label><button>Temporäres Passwort setzen</button></form></main>}
